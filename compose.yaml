name: fjordflix
services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8096:8080"
    gpus: all
    volumes:
      - fjordflix-data:/data
    environment:
      MAX_TRANSCODES: "3"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
volumes:
  fjordflix-data:
